function  button Alert () {
     alert(this is an alert)
}function addition (a, b) {
   alert)a + b)

 Function subtraction (a, b)) {
    alert (a - b)

    function multiplacation)a, b) {
        alert(a * b)
    
    Function division(a, b) {
        alert(a / b)
    }}
    Function caculator() {
       var operation = prompt("enter an operation")
       var a = promt("enter your number")
       var b = promt("enter your second number")

       If (operartion ==="addition")
       alert (aadditon(a,b) = "subtraction"
       else if (operation === "multiplacation"(a,b)
       else if (operation == "division")) )
    
       alert ("invalid operation")
       

    }
 }